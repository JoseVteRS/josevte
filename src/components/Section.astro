---
interface Props {
    title?: string;
}

const { title } = Astro.props;
---

<section class="mt-14">
    {title && <h2 class="mt-7 mb-2 text-xl font-semibold">{title}</h2>}
    <slot />
</section>
